import { SocialAccountDetailView } from "@/features/social-accounts/components/social-account-detail-view";

export function SocialAccountDetailPageView({ id }: { id: string }) {
  return <SocialAccountDetailView id={id} />;
}
